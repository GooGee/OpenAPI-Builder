enum DataType {
    boolean = 'boolean',
    enumer = 'enumer',
    integer = 'integer',
    number = 'number',
    object = 'object',
    reference = 'reference',
    string = 'string',
}

export const dataTypeList = Object.values(DataType)

export default DataType
